#include "values.h" //autonomous chooser
#include "menu.h" // user input
void initializeRobot ()
{
	//put stuff here later
	return;
}

task main()
{
	initializeRobot();
	//do auto stuff here. sideColor is set in menu.h
	if (sideColor == RED) {
		//do this
	}
	if (sideColor == BLUE) {
		//do this
	}
}
